{
	"name": "resume",
	"private": true,
	"scripts": {
		"test": "playwright test",
		"og": "node scripts/build-og.mjs",
		"check": "playwright test && node scripts/build-og.mjs",
		"cards": "node scripts/build-cards.mjs"
	},
	"devDependencies": {
		"@playwright/test": "^1.49.0"
	}
}
